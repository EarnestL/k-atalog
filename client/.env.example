# Katalog client – copy to .env and adjust (optional)
# See: https://vitejs.dev/guide/env-and-mode

# ---- Development (Vite dev server proxy) ----
# Where to proxy /api requests when running npm run dev. Default: http://127.0.0.1:8000
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# ---- API base URL (browser requests) ----
# Leave unset in dev to use the proxy (/api/v1 → backend).
# Set to call the backend directly: VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
# Production: set to your API origin, e.g. VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
# VITE_API_BASE_URL=
